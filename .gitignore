# Build output
bin/
obj/
out/
publish/
*.user
.vs/
.vscode/
.idea/

# Runtime artifacts (local dev runs)
logs/
data/
chat-*.json
roster.json
motd.txt

# CHANGELOG.md is internal commentary only — never push to GitHub. Lives
# locally at ../sundial-internal/CHANGELOG.md (symlinked here for convenience).
CHANGELOG.md

# packaging output
dist/

# Python bytecode from the local guard scripts
__pycache__/
*.pyc

# Native plugin build output and its pinned MinHook checkout. The release
# workflow produces both fresh; a committed copy is a binary nobody can
# reproduce from this tree.
native/SolarpunkPlugin/build/
native/SolarpunkPlugin/minhook/
